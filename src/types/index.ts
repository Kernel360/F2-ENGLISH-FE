export interface ListeningPreview {
  id: number;
  title: string;
  category: string;
  imageUrl: string;
}

export interface ReadingPreview {
  id: number;
  title: string;
  category: string;
  thumbnail: string;
  description: string;
}
